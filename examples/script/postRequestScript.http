
GET /json

{{
  const assert = require('assert');

  assert.equal(response.slideshow.title, 'Sample Slide Show', "Post Request Scripts Works");
}}

GET /json

{{
  const assert = require('assert');
  assert.equal(response.slideshow.title, 'fail', "Post Request Scripts does not Works");
}}